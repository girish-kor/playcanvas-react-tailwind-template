## What does this change?

## Why?

## How was this tested?

- [ ] `npm run check` passes locally (lint + format + tests)
- [ ] Tested in a browser (`npm run dev`) if this touches `scene/` or `ui/`

## Checklist

- [ ] No engine imports in `game/`
- [ ] No new console warnings
